<%= render 'shared/navbar' %>
 
<table>
    <tbody></tbody>
        <% @dogs.each do |dog| %>
           
           <div class="carde">
            <div class="card-product">
            <img src="" />
            <div class="card-product-infos">
                <%= dog.name %>
            <%= dog.description %>
            <%= link_to 'Show', dog %>
            </div>
            </div>
            </div>
                
        
        <% end %>
    </tbody>
</table>
  
